## 构造函数 / 析构函数 / 拷贝 / 赋值

可以在这里整理和实现这些问题：

- 为什么一般将 **析构函数设为虚函数**？
- **构造函数为什么通常不会做成一个虚函数？**
- 为什么构造函数一般写为 **初始化列表**？
- 什么是 **构造函数**、**析构函数**？默认构造、拷贝构造、移动构造？
- **内联的构造函数**可以怎么写？（可结合 inline）

建议：写几个有资源（比如动态内存、文件句柄）的类，分别只写普通析构、虚析构，观察多态删除时的行为差异。

