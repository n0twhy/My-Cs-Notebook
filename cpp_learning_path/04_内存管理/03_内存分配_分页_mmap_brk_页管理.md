## 内存分配、分页、mmap/brk 与页管理

可以在这里整理和实现这些问题：

- `malloc` 的原理？如何向操作系统申请更大的一块内存？
- `malloc 1KB` 和 `1MB` 有什么区别？可能走不同的系统调用路径？
- 介绍一下 `malloc` 的 `brk` / `mmap` 实现思路？
- 分页内存管理简单说一下：页、页表、多级页表的概念？

建议：写程序不断分配不同大小的内存，配合 `strace` 观察底层是否调用 `brk` 或 `mmap`，理解小块/大块分配策略。

