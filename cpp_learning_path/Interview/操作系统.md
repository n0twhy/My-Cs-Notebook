# 进程与线程
## 用户态 内核态
为了安全性，稳定性，隔离性
- 内核态：cpu可以执行所有指令和访问所有硬件资源，拥有极高的权限
- 用户态：主要用于运行用户程序，权限较低

## 进程  线程  协程
`进程类似于一个车间，线程类似于车间工人，CPU是工人调度器`
- 进程：是操作系统进行资源(内存空间，文件描述符，网络端口)与调度的基本单位，拥有独立的栈、堆空间。把物理地址分为若干页，然后进程建立虚拟地址映射这些页

- 线程：进程内的执行单元，操作系统进行CPU调度和执行的基本单位。多个线程共享栈空间，线程之间通信更高效。但是同时可能存在竞争

- 协程：用户态的轻量级线程。调度完全由用户决定，拥有自己的寄存器上下文，与其他协程共享，无需内核级的上下文切换

## 线程存在的意义
线程之间可以并发运行，共享相同的内存空间
### 多线程的优势 劣势
- 多线程并发进行，提升程序运行的效率
- 劣势是容易出现竞争访问的问题，还可能造成死锁。

## 切换

### 线程切换
非常像协程：会保存当前的寄存器信息，私有栈,TCB(线程控制块)作为上下文信息
调度器选择了下一个要执行的线程以后，从该线程保存的上下文信息中继续执行。
### 进程切换
首先进程有五种状态：创建状态，就绪状态，运行状态，阻塞状态，结束状态

- null -> 创建
- 创建 -> 就绪
- 就绪 -> 运行 被调度器选中，分配
- 运行 -> 结束
- 运行 -> 就绪 时间片用完，系统切换此进程为就绪状态，再从其他就绪状态的进程里选一个运行
- 运行 -> 阻塞 请求某个IO事件必须要等待，该进程切换为阻塞状态
- 阻塞 -> 就绪 等待事件完成，回到就绪状态

#### 进程的上下文
由于线程共享一个虚拟的内存空间，所以切换动作是内核只需要把当前线程的寄存器信息和私有栈保存起来就ok
但是进程之间并不共享，他有一个最重要的东西：页表
#### 页表
虚拟内存要找到真实物理地址，要查页表，但是我们需要的时间是一瞬间，所以CPU内部有一个极快的硬件缓存叫TLB(translation lookaside buffer)
当切换进程时，虚拟内存空间完全不一样了，需要将TLB全部清空。新进程刚运行一开始访问TLB都是未命中，所以需要老老实实查页表，直到TLB被重新填满

所以进程的切换效率相对线程来说是极慢的、

### 进程间的通信方式
- 管道
ps aux | grep xxx
分为匿名管道或者命名管道，只能父子进程或者兄弟进程来用
#### 命名管道 `fifo`
`mkfifo myfifo`
`echo > myfifo`
`cat < myfifo`

- 消息队列 
- 共享内存：两个进程各拿出一个虚拟地址指向同一个真实的物理地址
- 信号：操作系统通过硬件中断或者软件异常，直接打断当前的执行流，强行处理一个信号处理函数
- 信号量：类似于锁，进程间处理同步和互斥，与共享内存一起搭配
- `socket`不仅可以用于本地进程间通信，还可以用于网络中不同计算机上的进程通信

### 线程间的通信方式
线程之间天生享有同一块进程的虚拟内存，所以线程之间通信的核心应该在于同步和互斥
- 共享内存
- 互斥锁
- 条件变量,condition_variable
- 信号量

## 进程调度的算法
- 先来先服务FCFS
- 最短作业优先调度 SJF
- 高响应比优先调度 优先权 = (等待时间 + 要求服务时间) / 要求服务时间
- 时间片轮转

## 协程 Coroutine
线程上下文是由操作系统内核来保存、恢复的，需要从用户态切换到内核态，开销很大。
协程上下文是用户态，非常轻量

## 僵尸进程  孤儿进程
- 父进程与子进程。子进程已经`exit()`资源已经全部释放和析构，但是父进程没有`wait()`来检查子进程的状态，这时候操作系统为了让父进程能感知到子进程的状态，把子进程的状态改为Z(Zombie).后果是为占一个pid号

- 孤儿进程就是，父进程死了，但是子进程还在运行。这是pid为1的"孤儿院院长"就会把这个孤儿进程安排到自己下边


# 内存管理
## 虚拟内存优势
- 可以大于真实物理内存，因为可以把那些基本不用的内存，放到硬盘里
- 页表私有，解决进程间地址冲突的问题

### 页表
根据b+树的页和硬盘地址的关系来理解

## 程序的内存布局
高地址
+---------------------------+
|          stack            |  ← 局部变量、函数栈帧（一般向低地址增长）
|            ·              |
|            ·              |
+---------------------------+
|          (空洞)            |
|            ·              |
+---------------------------+
|          heap             |  ← new/malloc（一般向高地址增长）
+---------------------------+
|   .bss（未初始化全局/静态）  |
+---------------------------+
|   .data（已初始化全局/静态） |
+---------------------------+
|   .text（机器码，常数区等）  |
+---------------------------+
低地址

空洞中间有一个专门的文件映射段mmap，包括动态库，创建子线程的私有栈，共享内存，大内存的malloc(malloc并不是系统调用，而是c的库函数)

# 中断
## 类型
- 外部中断：来源于cpu外部，比如网卡，分为可屏蔽中断和不可屏蔽中断
- 内部中断：来自处理器内部
软中断：软件主动发起的中断
异常则是cpu内部发生的错误

## 作用
使计算机拥有了应对处理突发事件的能力
# IO模型
## 种类
- 阻塞
- 非阻塞 发起IO操作立即返回，一直轮询数据是否准备好，
- IO复用模型 : select, poll, epoll，系统在等待事件完成后会通知完成
- 信号驱动IO:和复用模型的区别就是复用的时候，原程序还是等待状态，信号则是直接开始做其他工作
- 异步IO模型：程序完全不用管，系统自己拷贝好
## 多路复用
是一种IO处理方式，是指在一个线程中，可以处理多个socket中的事件，能够资源复用，防止创造过多线程导致上下文切换的开销
### select, poll, epoll
这是三个多路复用的接口
select -> poll -> epoll
本质上是消除三个瓶颈，fd数量限制，全量拷贝，内核O(n)遍历

#### select
将已连接的socket都放在一个fd文件集合，然后把这个集合拷贝在内核里，内核通过遍历来检查事件。如果事件发生，内核标记为可读或者可写，接着再把集合拷贝回用户态里，再遍历一遍找到可读或者可写的socket。

2次遍历，两次拷贝，用bitsmap
#### poll
没有数量限制了
与select大差不差，把bitsmap换成数组的形式了
#### epoll
fd文件集合是以红黑树的形式存储，增删改的复杂度是O(logn)，select和poll每次都要传整个集合，而epoll只需要插入即可
而且有就绪事件链表，不需要再for循环遍历

##### 为什么用红黑树，哈希表不可以吗
哈希表O(1)，但是高并发状态下连接多了，rehash开销太大

##### ET vs LT
- ET：状态切换的时候才触发
- LT：保持在某个状态就会触发

ET在epoll这里要注意，必须一次性全部读完，不然的话里边剩的就再也醒不过来了
ET对比LT的好处：一次全部处理完，可以少几次sys_call，他只在变化时通知一次
# 零拷贝
## 传统IO方式
磁盘文件拷贝到内核态buffer(这里是DMA)，用户态read内核态的buffer(1次内存切换)(这里是CPU)，切换回用户态(又1次)。
用户态write内核态的socket,又是两次切换。socket通过DMA拷贝到网卡
## 零拷贝
零拷贝则是，cpu不需要搬任何东西了，内核态buffer直接通过sg dma传到网卡那里


