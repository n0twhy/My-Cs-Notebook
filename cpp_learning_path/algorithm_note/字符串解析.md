[](../../algorithm/huawei/多段数据下发.cpp)
stringstream: getline(ss, token, 分隔符)
ss >> token 读取以空格为分隔符的字符串
ss.str() 变成string
ss.clear() 重置

substr: 若省略后面那个len，则会从下标一直到结尾
s.find 如果没找到，返回string::npos(size_t类型)
s.find('', pos) pos就是代表从pos开始找

(auto& [l, r] : v) 快速用pair给l，r赋值

[getline](../../algorithm/huawei/多段数据下发.cpp#L176)
cin.ignore()

[十进制转为二进制](../../algorithm/huawei/查找可用内存块.cpp)
bitset<8>(i).to_string()