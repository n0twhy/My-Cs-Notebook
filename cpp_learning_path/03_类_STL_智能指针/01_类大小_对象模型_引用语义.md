## 类大小、对象模型与引用相关问题

可以在这里整理和实现这些问题：

- C++ 空类的大小是多少？为什么不是 0？
- 只含虚函数的类的大小是多少？虚函数表指针带来的影响？
- 一个只包含 int 成员的空 class 和只包含 int 成员的空 struct 的内存大小分别是多少？（本质相同）
- 引用本质上是什么？和指针在实现层面有何不同？

建议：用 `sizeof` 测试不同类/struct 的大小，配合 `objdump` 或调试器观察对象内存布局。

