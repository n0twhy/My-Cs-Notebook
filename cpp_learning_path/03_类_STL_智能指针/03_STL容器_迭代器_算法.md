## STL 容器、迭代器与算法

可以在这里整理和实现这些问题：

- `vector` 中 `push_back` 和 `emplace_back` 的区别？
- C++ 中 `map` 的底层数据结构是什么？`unordered_map` 的底层结构是什么？
- 容器可以一边遍历一边插入/删除吗？不同容器的行为差异？
- 使用迭代器怎么删除一个元素？哪些操作会导致迭代器失效？
- STL 容器中优先级队列 `priority_queue` 的底层原理？
- 双向队列 `deque` 是如何实现的？
- `std::sort` 的底层是怎么实现的？（混合排序、快速排序/插入排序等）

建议：针对每种容器写小测试，模拟插入/删除/遍历，观察迭代器、引用是否失效，体会复杂度。

1.push_back会拷贝而emplace_back是直接在尾部创建这个元素