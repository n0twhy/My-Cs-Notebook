# 进程与线程
## 用户态 内核态
为了安全性，稳定性，隔离性
- 内核态：cpu可以执行所有指令和访问所有硬件资源，拥有极高的权限
- 用户态：主要用于运行用户程序，权限较低

## 进程  线程  协程

- 进程：是操作系统进行资源与调度的基本单位，拥有独立的栈、堆空间

- 线程：进程内的执行单元，多个线程共享栈空间，线程之间通信更高效。但是同时可能存在竞争

- 协程：用户态的轻量级线程。调度完全由用户决定，拥有自己的寄存器上下文，与其他协程共享，无需内核级的上下文切换

## 线程存在的意义

