## C++11 新特性：auto / 类型推导 / lambda / nullptr / =default / =delete

可以在这里整理和实现这些问题：

- C++11 引入了哪些重要新特性？（结合本学习路径选几个最常用的）
- `auto` 类型推导的原理和使用注意点？
- `decltype`、模板类型推导与 `auto` 的关系（可选深入）？
- `lambda` 表达式的原理是什么？捕获列表、可变 lambda、泛型 lambda 等。
- `=default` 和 `=delete` 函数的区别是什么？应用场景？
- C++11 中 `nullptr` 比 `NULL` 有什么优势？

建议：写很多小例子用 `auto`、lambda、`=default`/`=delete`、`nullptr` 替换旧写法，对比可读性与错误提示。

